import 'package:flutter/material.dart';

class AppColors {
  // Primary — deep forest green
  static const Color primary = Color(0xFF1B5E38);
  static const Color primaryDark = Color(0xFF0E3D22);
  static const Color primaryLight = Color(0xFF2E8B57);

  // Accent — warm golden amber (financial highlights)
  static const Color accent = Color(0xFFC8963E);
  static const Color accentLight = Color(0xFFE8B96A);

  // Background colors — warm off-white tones
  static const Color background = Color(0xFFF4F7F4);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFEBF4EE);

  // Text colors — green-tinted dark palette
  static const Color textPrimary = Color(0xFF1A2B22);
  static const Color textSecondary = Color(0xFF4A6458);
  static const Color textTertiary = Color(0xFF7A9688);

  // Status colors
  static const Color success = Color(0xFF2E8B57);
  static const Color error = Color(0xFFC0392B);
  static const Color warning = Color(0xFFD4782A);
  static const Color info = Color(0xFF2471A3);

  // Border colors
  static const Color border = Color(0xFFC4D8CC);
  static const Color borderDark = Color(0xFF9BBDAA);

  // Profit / Loss
  static const Color profit = Color(0xFF27AE60);
  static const Color loss = Color(0xFFC0392B);

  // Card shadow
  static const Color shadow = Color(0x1A1B5E38);
}
